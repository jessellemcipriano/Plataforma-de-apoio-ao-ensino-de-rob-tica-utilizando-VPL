Blockly.JavaScript['sensortemperatura'] = function (block) {
  var variable = 'sensorTemperatura';
  return [variable, 99];
};

Blockly.JavaScript['sensorluz1'] = function (block) {
  var variable = 'sensorluminosidade1';
  return [variable, 99];
};

Blockly.JavaScript['sensorluz2'] = function (block) {
  var variable = 'sensorluminosidade2';
  return [variable, 99];
};


Blockly.JavaScript['sensorultrassonico'] = function (block) {
  var variable = 'sensorultrassonico';
  return [variable, 99];
};

Blockly.JavaScript['sensorindutivo'] = function (block) {
  var variable = 'sensorindutivo';
  return [variable, 99];
};

Blockly.JavaScript['sensorvelocidade'] = function (block) {
  var variable = 'sensorvelocidade';
  return [variable, 99];
}

Blockly.JavaScript['sensorumidade'] = function (block) {
  var variable = 'sensorumidade';
  return [variable, 99];
}